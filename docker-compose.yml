services:
  db:
    image: mysql:8.0
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: projeto_integrador
      MYSQL_USER: user
      MYSQL_PASSWORD: password
    ports:
      - "3306:3306"